# Price API (FastAPI) — Render Ready
Minimal FastAPI service with one endpoint `/search`.

**Render setup**
- Build command: `pip install -r requirements.txt`
- Start command: `python -m uvicorn main:app --host 0.0.0.0 --port $PORT`
